name: Release

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'lw-site-manager.php'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Extract version from plugin file
        id: get_version
        run: |
          VERSION=$(grep -m1 "Version:" lw-site-manager.php | sed 's/.*Version:[[:space:]]*//' | tr -d '[:space:]')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Check if release exists
        id: check_release
        env:
          VERSION: ${{ steps.get_version.outputs.version }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view "v$VERSION" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Release v$VERSION already exists — skipping"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Release v$VERSION does not exist — will create (even if the tag was pushed manually)"
          fi

      - name: Setup PHP
        if: steps.check_release.outputs.exists == 'false'
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Install production dependencies
        if: steps.check_release.outputs.exists == 'false'
        run: composer install --no-dev --optimize-autoloader --no-interaction

      # The MCP adapter is a dev-only Composer dependency so that a
      # `composer require lwplugins/lw-site-manager` does NOT drag it into
      # consumer projects (on Bedrock it is type:wordpress-plugin and would
      # install as a stray plugin). The wordpress.org ZIP still bundles it here
      # so the built-in MCP server keeps working out of the box.
      - name: Bundle the MCP adapter into the release ZIP
        if: steps.check_release.outputs.exists == 'false'
        run: composer require wordpress/mcp-adapter:^0.5 --update-no-dev --optimize-autoloader --no-interaction

      - name: Create release ZIP
        if: steps.check_release.outputs.exists == 'false'
        env:
          VERSION: ${{ steps.get_version.outputs.version }}
        run: |
          mkdir -p dist/lw-site-manager
          rsync -av --exclude='.git' --exclude='.github' --exclude='dist' \
            --exclude='builds' --exclude='composer.json' --exclude='composer.lock' \
            --exclude='phpcs.xml.dist' --exclude='node_modules' --exclude='.gitignore' \
            --exclude='tests' --exclude='phpstan.neon.dist' --exclude='phpstan-baseline.neon' \
            --exclude='phpstan-bootstrap.php' --exclude='phpunit.xml.dist' \
            --exclude='phpunit-integration.xml.dist' --exclude='.claude' \
            --exclude='*.log' . dist/lw-site-manager/
          cd dist && zip -r "lw-site-manager-${VERSION}.zip" lw-site-manager/

      - name: Create tag and release
        if: steps.check_release.outputs.exists == 'false'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ steps.get_version.outputs.version }}
          name: v${{ steps.get_version.outputs.version }}
          body: |
            ## LW Site Manager v${{ steps.get_version.outputs.version }}

            ### Installation

            **Via Composer:**
            ```bash
            composer require lwplugins/lw-site-manager
            ```

            **Manual Installation:**
            1. Download `lw-site-manager-${{ steps.get_version.outputs.version }}.zip`
            2. Upload to `/wp-content/plugins/`
            3. Activate in WordPress admin

            ### Changelog

            See [readme.txt](https://github.com/lwplugins/lw-site-manager/blob/main/readme.txt) for full changelog.
          files: |
            dist/lw-site-manager-${{ steps.get_version.outputs.version }}.zip
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
